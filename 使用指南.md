# 📖 个性化手写输入法APP - 使用指南

## 🚀 快速开始（5分钟上手）

### 方法一：使用快速启动脚本（推荐）

1. **双击运行** `快速启动.bat`
2. 选择 `2. 安装开发工具` - 按照提示安装MySQL、Maven、Flutter
3. 选择 `3. 安装Redis` - 安装Redis（可选）
4. 选择 `1. 检查环境配置` - 确认所有工具已安装
5. 选择 `4. 初始化数据库` - 输入MySQL root密码
6. 选择 `5. 配置阿里云OSS` - 输入OSS信息（可选）
7. 选择 `6. 启动项目` - 启动前后端服务

### 方法二：手动配置

如果您更喜欢手动配置，请参考以下步骤：

## 🔧 详细配置步骤

### 第一步：安装必需工具

#### 1.1 安装MySQL 8.0

**下载地址**：https://dev.mysql.com/downloads/mysql/

**安装步骤**：
1. 下载 `mysql-installer-community-8.0.xx.x.msi`
2. 运行安装程序
3. 选择 "Developer Default"
4. 设置root密码（**请记住这个密码！**）
5. 完成安装
6. 打开命令提示符，输入 `mysql --version` 验证安装

#### 1.2 安装Maven

**下载地址**：https://maven.apache.org/download.cgi

**安装步骤**：
1. 下载 `apache-maven-3.9.x-bin.zip`
2. 解压到 `C:\Program Files\Maven\`
3. 配置环境变量：
   - 右键"此电脑" → 属性 → 高级系统设置 → 环境变量
   - 新建系统变量：
     - 变量名：`MAVEN_HOME`
     - 变量值：`C:\Program Files\Maven`
   - 编辑Path变量，添加：`%MAVEN_HOME%\bin`
4. 打开新的命令提示符，输入 `mvn -version` 验证安装

#### 1.3 安装Flutter

**下载地址**：https://flutter.dev/docs/get-started/install/windows

**安装步骤**：
1. 下载Flutter SDK压缩包
2. 解压到 `C:\src\flutter\`
3. 配置环境变量：
   - 编辑Path变量，添加：`C:\src\flutter\bin`
4. 打开新的命令提示符，输入 `flutter --version` 验证安装
5. 运行 `flutter doctor` 检查环境（可能需要安装Android Studio）

#### 1.4 安装Redis（可选）

**下载地址**：https://github.com/microsoftarchive/redis/releases

或者运行：
```bash
scripts\install-redis.bat
```

### 第二步：初始化数据库

运行脚本：
```bash
scripts\init-database.bat
```

按照提示输入MySQL root密码，脚本会自动完成：
- 创建数据库 `handwriting_input_app`
- 创建用户 `handwriting`
- 导入初始化数据

### 第三步：配置阿里云OSS（可选）

**注册阿里云**（免费）：
1. 访问：https://www.aliyun.com/
2. 注册账号（需要手机号验证）

**开通OSS服务**（3个月免费）：
1. 访问：https://oss.console.aliyun.com/
2. 点击"立即开通"
3. 新用户享受3个月免费试用

**创建Bucket**：
1. 点击"创建Bucket"
2. 填写配置：
   - Bucket名称：`handwriting-input-app`
   - 地域：华东1（杭州）
   - 存储类型：标准存储
   - 读写权限：私有
3. 点击确定

**获取AccessKey**：
1. 访问：https://ram.console.aliyun.com/manage/ak
2. 创建AccessKey
3. 记录 `AccessKey ID` 和 `AccessKey Secret`

**配置CORS规则**（重要！）：
1. 进入Bucket管理 → 权限管理 → 跨域设置
2. 创建规则：
   - 来源：`*`
   - 允许Methods：`GET, POST, PUT, DELETE, HEAD`
   - 允许Headers：`*`
   - 暴露Headers：`ETag, x-oss-request-id`
   - 缓存时间：`600`
3. 点击确定

**更新配置**：
运行脚本：
```bash
scripts\config-oss.bat
```

按照提示输入：
- AccessKey ID
- AccessKey Secret
- Bucket名称

### 第四步：启动项目

#### 方式一：同时启动前后端
```bash
scripts\start.bat
```

#### 方式二：分别启动

启动后端（在新窗口）：
```bash
scripts\start-backend.bat
```

启动前端（在新窗口）：
```bash
scripts\start-frontend.bat
```

## 📱 使用应用

### 1. 注册账号

首次使用需要注册：
- 打开Flutter应用
- 点击"还没有账号？去注册"
- 输入手机号（任意11位数字）
- 设置密码（至少6位）
- 点击"注册"

### 2. 登录系统

- 输入手机号和密码
- 点击"登录"

### 3. 手写采集

- 点击底部"手写采集"标签
- 在画布上手写汉字
- 点击"保存"上传笔迹
- 查看已上传的笔迹列表

### 4. 字体管理

- 点击底部"字体管理"标签
- 点击"+"号生成新字体
- 输入字体名称
- 等待字体生成
- 查看字体预览
- 设置默认字体

### 5. 个人中心

- 点击底部"我的"标签
- 查看用户信息
- 退出登录

## 🔍 常见问题

### Q1: MySQL连接失败

**症状**：初始化数据库时提示连接失败

**解决方法**：
1. 确认MySQL服务已启动
   - 打开"服务"（services.msc）
   - 找到"MySQL80"
   - 右键 → 启动
2. 确认root密码正确
3. 确认端口为3306

### Q2: Maven下载依赖慢

**解决方法**：
配置国内镜像源，编辑 `C:\Users\用户名\.m2\settings.xml`：
```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

### Q3: Flutter设备未连接

**解决方法**：
1. 运行 `flutter devices` 查看可用设备
2. 连接Android设备（开启USB调试）
3. 或使用Android模拟器

### Q4: 文件上传失败

**解决方法**：
1. 确认OSS配置正确
2. **重要**：检查CORS规则是否配置
3. 确认Bucket名称正确
4. 确认AccessKey有效

### Q5: 后端启动失败

**解决方法**：
1. 检查数据库是否初始化
2. 检查配置文件是否正确
3. 查看日志文件：`backend/logs/handwriting-input-app.log`
4. 检查端口8080是否被占用

## 📊 默认配置

| 配置项 | 值 |
|--------|-----|
| 数据库名 | handwriting_input_app |
| 数据库用户 | handwriting |
| 数据库密码 | handwriting123 |
| 后端端口 | 8080 |
| 前端端口 | 随机（Flutter分配） |
| Redis端口 | 6379 |
| API文档 | http://localhost:8080/swagger-ui.html |

## 🎯 测试账号

首次使用需要自己注册，系统没有预设测试账号。

## 💡 优化建议

### 提高Maven下载速度

使用阿里云Maven镜像：
```xml
<mirror>
  <id>aliyun</id>
  <mirrorOf>central</mirrorOf>
  <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

### 提高Flutter下载速度

设置国内镜像源：
```bash
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn
```

Windows:
```cmd
set PUB_HOSTED_URL=https://pub.flutter-io.cn
set FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn
```

## 📞 获取帮助

如果遇到问题：

1. **查看日志**
   - 后端日志：`backend/logs/handwriting-input-app.log`
   - 前端日志：Flutter控制台

2. **查看文档**
   - `QUICK_CONFIG_GUIDE.md` - 快速配置指南
   - `docs/TODO_NEED_USER_COLLABORATION.md` - 配置说明
   - `PROJECT_SUMMARY.md` - 项目总结

3. **检查环境**
   - 运行 `scripts\check-config.bat`

## 🎉 开始使用

现在您可以：
1. 双击 `快速启动.bat`
2. 按照菜单操作
3. 开始使用个性化手写输入法APP

祝您使用愉快！🚀

---

**最后更新**: 2026-01-30
**文档版本**: 1.0.0